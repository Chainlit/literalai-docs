Represents a thread in the system, extending the properties and methods from `ThreadFields`.
This class manages thread-specific operations such as creation and updates via the API.

## Extends

- `ThreadFields`

## Constructors

#### new Thread(client, data)

```ts
new Thread(client, data?): Thread
```

Constructs a new Thread instance, with a generated ID if none is provided.

**Parameters**

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `client` | `LiteralClient` | - |
| `data`? | `ThreadConstructor` | Optional initial data for the thread. |

**Returns**

[`Thread`](README.md#thread)

**Overrides**

ThreadFields.constructor

## Properties

| Property | Type | Description | Inheritance |
| :------ | :------ | :------ | :------ |
| `id` | `string` | - | ThreadFields.id |
| `participantId`? | `Maybe`\<`string`\> | - | ThreadFields.participantId |
| `environment`? | `Maybe`\<`Environment`\> | - | ThreadFields.environment |
| `name`? | `Maybe`\<`string`\> | - | ThreadFields.name |
| `metadata`? | `Maybe`\<`Record`\<`string`, `any`\>\> | - | ThreadFields.metadata |
| `steps`? | `Maybe`\<`Step`[]\> | - | ThreadFields.steps |
| `tags`? | `Maybe`\<`string`[]\> | - | ThreadFields.tags |
| `api` | `API` | - | - |
| `client` | `LiteralClient` | - | - |

## Methods

### step()

```ts
step(data): Step
```

Creates a new step associated with this thread.

**Parameters**

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `data` | `Omit`\<`StepConstructor`, `"threadId"`\> | The data for the new step, excluding the thread ID. |

**Returns**
| type | description |
| :------ | :------ |
| `Step` | A new Step instance linked to this thread |

### run()

```ts
run(data): Step
```

Creates a new Run step associated with this thread.

**Parameters**

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `data` | `Omit`\<`StepConstructor`, `"type"` \| `"threadId"`\> | The data for the new step, excluding the thread ID and the type |

**Returns**
| type | description |
| :------ | :------ |
| `Step` | A new Step instance linked to this thread |

### upsert()

```ts
upsert(): Promise<Thread>
```

Upserts the thread data to the backend, creating or updating as necessary.

**Returns**
| type | description |
| :------ | :------ |
| `Promise`\<[`Thread`](README.md#thread)\> | The updated Thread instance |

### wrap()

```ts
wrap<Output>(cb, updateThread?): Promise<Output>
```

Sends the thread to the API, handling disabled state and setting the end time if not already set.

**Type parameters**

| Parameter |
| :------ |
| `Output` |

**Parameters**

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `cb` | (`thread`) => `Output` \| `Promise`\<`Output`\> | The callback function to run within the context of the thread. |
| `updateThread`? | `ThreadConstructor` \| (`output`) => `ThreadConstructor` \| (`output`) => `Promise`\<`ThreadConstructor`\> | Optional update function to modify the thread after the callback. |

**Returns**
| type | description |
| :------ | :------ |
| `Promise`\<`Output`\> | The output of the wrapped callback function |

### serialize()

```ts
serialize(): any
```

Serializes the properties of the current instance into a dictionary, excluding the 'api' property.
It handles nested objects that also implement a serialize method.

**Returns**
| type | description |
| :------ | :------ |
| `any` | A dictionary representing the serialized properties of the object |

**Inherited from**

ThreadFields.serialize

***
