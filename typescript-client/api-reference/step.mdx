Represents a step in a process or workflow, extending the fields and methods from StepFields.

## Extends

- `StepFields`

## Constructors

#### new Step(client, data, ignoreContext)

```ts
new Step(
   client, 
   data, 
   ignoreContext?): Step
```

Constructs a new Step instance.

**Parameters**

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `client` | `LiteralClient` | - |
| `data` | `StepConstructor` | The initial data for the step, excluding utility properties. |
| `ignoreContext`? | `true` | - |

**Returns**

[`Step`](README.md#step)

**Overrides**

StepFields.constructor

## Properties

| Property | Type | Description | Inheritance |
| :------ | :------ | :------ | :------ |
| `name` | `string` | - | StepFields.name |
| `type` | [`StepType`](README.md#steptype) | - | StepFields.type |
| `threadId`? | `string` | - | StepFields.threadId |
| `rootRunId`? | `Maybe`\<`string`\> | - | StepFields.rootRunId |
| `createdAt`? | `Maybe`\<`string`\> | - | StepFields.createdAt |
| `startTime`? | `Maybe`\<`string`\> | - | StepFields.startTime |
| `id`? | `Maybe`\<`string`\> | - | StepFields.id |
| `environment`? | `Maybe`\<`Environment`\> | - | StepFields.environment |
| `error`? | `Maybe`\<`string` \| `Record`\<`string`, `any`\>\> | - | StepFields.error |
| `input`? | `Maybe`\<`Record`\<`string`, `any`\>\> | - | StepFields.input |
| `output`? | `Maybe`\<`Record`\<`string`, `any`\>\> | - | StepFields.output |
| `metadata`? | `Maybe`\<`Record`\<`string`, `any`\>\> | - | StepFields.metadata |
| `tags`? | `Maybe`\<`string`[]\> | - | StepFields.tags |
| `parentId`? | `Maybe`\<`string`\> | - | StepFields.parentId |
| `endTime`? | `Maybe`\<`string`\> | - | StepFields.endTime |
| `generation`? | `Maybe`\<`Generation`\> | - | StepFields.generation |
| `scores`? | `Maybe`\<`Score`[]\> | - | StepFields.scores |
| `attachments`? | `Maybe`\<`Attachment`[]\> | - | StepFields.attachments |
| `api` | `API` | - | - |
| `client` | `LiteralClient` | - | - |

## Methods

### serialize()

```ts
serialize(): any
```

Serializes the step instance, converting complex objects to strings as necessary.

**Returns**
| type | description |
| :------ | :------ |
| `any` | A serialized representation of the step |

**Overrides**

StepFields.serialize

### isMessage()

```ts
isMessage(): boolean
```

Determines if the step is a type of message.

**Returns**
| type | description |
| :------ | :------ |
| `boolean` | True if the step is a user, assistant, or system message |

### step()

```ts
step(data): Step
```

Creates a new step instance linked to the current step as a parent.

**Parameters**

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `data` | `Omit`\<`StepConstructor`, `"threadId"`\> | The data for the new step, excluding the threadId which is inherited. |

**Returns**
| type | description |
| :------ | :------ |
| [`Step`](README.md#step) | A new Step instance |

### send()

```ts
send(): Promise<Step>
```

Sends the step to the API, handling disabled state and setting the end time if not already set.

**Returns**
| type | description |
| :------ | :------ |
| `Promise`\<[`Step`](README.md#step)\> | The current Step instance after potentially sending to the API |

### wrap()

```ts
wrap<Output>(cb, updateStep?): Promise<Output>
```

Sends the step to the API, handling disabled state and setting the end time if not already set.

**Type parameters**

| Parameter |
| :------ |
| `Output` |

**Parameters**

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `cb` | (`step`) => `Output` \| `Promise`\<`Output`\> | The callback function to run within the context of the step. |
| `updateStep`? | `Partial`\<`StepConstructor`\> \| (`output`) => `Partial`\<`StepConstructor`\> \| (`output`) => `Promise`\<`Partial`\<`StepConstructor`\>\> | Optional update function to modify the step after the callback. |

**Returns**
| type | description |
| :------ | :------ |
| `Promise`\<`Output`\> | The output of the wrapped callback function |

## Type Aliases

### StepType

```ts
type StepType: 
  | "assistant_message"
  | "embedding"
  | "llm"
  | "rerank"
  | "retrieval"
  | "run"
  | "system_message"
  | "tool"
  | "undefined"
  | "user_message";
```

***
