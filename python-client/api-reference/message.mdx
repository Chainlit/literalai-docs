<a id="literalai.message"></a>


<a id="literalai.message.Message"></a>

## Message

```python
class Message(Utils)
```

Class representing a message in the system.

**Attributes**:

<ResponseField name="id" type="Optional[str]">The unique identifier for the message. Defaults to None.</ResponseField>
<ResponseField name="name" type="Optional[str]">The name of the message. Defaults to None.</ResponseField>
<ResponseField name="type" type="Optional[MessageStepType]">The type of the message. Defaults to None.</ResponseField>
<ResponseField name="metadata" type="Optional[Dict]">The metadata associated with the message. Defaults to {}.</ResponseField>
<ResponseField name="parent_id" type="Optional[str]">The identifier of the parent message. Defaults to None.</ResponseField>
<ResponseField name="timestamp" type="Optional[str]">The timestamp of the message. Defaults to None.</ResponseField>
<ResponseField name="content" type="str">The content of the message.</ResponseField>
<ResponseField name="thread_id" type="Optional[str]">The identifier of the thread the message belongs to. Defaults to None.</ResponseField>
<ResponseField name="tags" type="Optional[List[str]]">The tags associated with the message. Defaults to None.</ResponseField>
<ResponseField name="created_at" type="Optional[str]">The time when the message was created. Defaults to None.</ResponseField>
<ResponseField name="scores" type="List[Score]">The scores associated with the message. Defaults to [].</ResponseField>
<ResponseField name="attachments" type="List[Attachment]">The attachments associated with the message. Defaults to [].</ResponseField>

<a id="literalai.message.Message.end"></a>

#### end

```python
def end()
```

Ends the message processing and adds the event to the processor.

<a id="literalai.message.Message.to_dict"></a>

#### to\_dict

```python
def to_dict() -> "StepDict"
```

Converts the Message object to a StepDict dictionary.

**Returns**:

<ResponseField name="StepDict">The dictionary representation of the Message object.</ResponseField>

<a id="literalai.message.Message.from_dict"></a>

#### from\_dict

```python
@classmethod
def from_dict(cls, message_dict: Dict) -> "Message"
```

Creates a Message object from a dictionary.

**Arguments**:

<ResponseField name="message_dict" type="Dict">The dictionary representation of the Message object.</ResponseField>
  

**Returns**:

<ResponseField name="Message">The Message object created from the dictionary.</ResponseField>

