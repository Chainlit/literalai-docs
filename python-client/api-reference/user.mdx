---
title: User
description: "Create Read Update and Delete methods for User"
---

## Create an User

```python
from chainlit_client import ChainlitClient

client = ChainlitClient()
new_user = client.api.create_user(identifier="John Doe")
```

<ParamField path="identifier" type="string" required>
  The identifier of the user
</ParamField>

### Return type

<ResponseField name="user" type="User">
  Return the user
</ResponseField>

## Get an User

```python
user = client.api.get_user(id="<USER_UUID>")
```

<ParamField path="id" type="uuid" required>
  The id of the user
</ParamField>

### Return type

<ResponseField name="user" type="User">
  Return the user, can be None if the user does not exist
</ResponseField>

## Update an User

```python
updated_user = client.api.update_user(id="<USER_UUID>", identifier="Jane Doe")
```

<ParamField path="id" type="uuid" required>
  The id of the user
</ParamField>

<ParamField path="identifier" type="string">
  The identifier of the user
</ParamField>

<ParamField path="metadata" type="dict">
  The metadata of the user
</ParamField>

### Return type

<ResponseField name="user" type="User">
  Return the user
</ResponseField>

## Delete an User

```python
user = client.api.delete_user(id="<USER_UUID>")
```

<ParamField path="id" type="uuid" required>
  The id of the user
</ParamField>
