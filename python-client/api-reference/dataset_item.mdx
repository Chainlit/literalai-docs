<a id="literalai.dataset_item"></a>


<a id="literalai.dataset_item.DatasetItemDict"></a>

## DatasetItemDict

```python
class DatasetItemDict(TypedDict)
```

A dictionary representation of a DatasetItem. All attributes are optional.

**Attributes**:

<ResponseField name="id" type="str">The unique identifier for the dataset item.</ResponseField>
<ResponseField name="createdAt" type="str">The timestamp when the dataset item was created.</ResponseField>
<ResponseField name="datasetId" type="str">The identifier of the dataset the item belongs to.</ResponseField>
<ResponseField name="metadata" type="Dict[str, Any]">Additional metadata for the dataset item.</ResponseField>
<ResponseField name="input" type="Dict[str, Any]">The input data for the dataset item.</ResponseField>
<ResponseField name="expectedOutput" type="Dict[str, Any]">The expected output data for the dataset item.</ResponseField>
<ResponseField name="intermediarySteps" type="List[Dict[str, Any]]">The intermediary steps for the dataset item.</ResponseField>

<a id="literalai.dataset_item.DatasetItem"></a>

## DatasetItem

```python
@dataclass(repr=False)
class DatasetItem(Utils)
```

A class representing a dataset item.

**Attributes**:

<ResponseField name="id" type="str">The unique identifier for the dataset item.</ResponseField>
<ResponseField name="created_at" type="str">The timestamp when the dataset item was created.</ResponseField>
<ResponseField name="dataset_id" type="str">The identifier of the dataset the item belongs to.</ResponseField>
<ResponseField name="metadata" type="Dict[str, Any]">Additional metadata for the dataset item.</ResponseField>
<ResponseField name="input" type="Dict[str, Any]">The input data for the dataset item.</ResponseField>
<ResponseField name="expected_output" type="Optional[Dict[str, Any]]">The expected output data for the dataset item. Defaults to None.</ResponseField>
<ResponseField name="intermediary_steps" type="Optional[List[Dict[str, Any]]]">The intermediary steps for the dataset item. Defaults to None.</ResponseField>

<a id="literalai.dataset_item.DatasetItem.to_dict"></a>

#### to\_dict

```python
def to_dict()
```

Converts the DatasetItem object to a DatasetItemDict dictionary.

**Returns**:

<ResponseField name="DatasetItemDict">The dictionary representation of the DatasetItem object.</ResponseField>

<a id="literalai.dataset_item.DatasetItem.from_dict"></a>

#### from\_dict

```python
@classmethod
def from_dict(cls, dataset_item: DatasetItemDict) -> "DatasetItem"
```

Creates a DatasetItem object from a DatasetItemDict dictionary.

**Arguments**:

<ResponseField name="dataset_item" type="DatasetItemDict">The dictionary representation of the DatasetItem object.</ResponseField>
  

**Returns**:

<ResponseField name="DatasetItem">The DatasetItem object created from the dictionary.</ResponseField>

